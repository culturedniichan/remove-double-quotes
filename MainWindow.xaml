<Window x:Class="remove_double_quotes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:remove_double_quotes"
        mc:Ignorable="d"
        Title="DOUBLE QUOTE REMOVER" Height="450" Width="800" Background="LightGray"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- Row for the label -->
            <RowDefinition Height="*" />
            <!-- Row for the TextBox -->
        </Grid.RowDefinitions>

        <Label Content="PASTE TEXT HERE" HorizontalAlignment="Left" />

        <TextBox Grid.Row="1" AcceptsReturn="True" TextWrapping="Wrap"
             VerticalScrollBarVisibility="Auto"
             HorizontalScrollBarVisibility="Auto"
             MinLines="3"
             Margin="4"
            TextChanged="TextBox_TextChanged"
            GotKeyboardFocus="TextBox_GotKeyboardFocus" />
    </Grid>
</Window>